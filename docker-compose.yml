services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: packetpath
      POSTGRES_PASSWORD: packetpath
      POSTGRES_DB: packetpath
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
